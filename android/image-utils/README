=========================================================================================
Android image utils
=========================================================================================

This are some of the android image utils with an automake environment. It's synchronized
from time to time with the upstream sources from the Android Open Source Project.

The following utilities are included:
- fastboot
- mkbootimg
- unbootimg

To successfully build the sources you need the following dependencies:
- zlib
